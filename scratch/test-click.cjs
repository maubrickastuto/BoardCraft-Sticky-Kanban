const CDP = require('chrome-remote-interface'); async function run() { let c; try { c = await CDP({port: 9222}); await c.Runtime.enable(); const res = await c.Runtime.evaluate({ expression: document.querySelector('#new-workspace-btn-empty').click(); setTimeout(() = const i = document.querySelector('#inline-workspace-name-input'); i.value = 'Test WS'; const evt = new Event('change', {bubbles: true}); i.dispatchEvent(evt); document.querySelector('#inline-workspace-form').dispatchEvent(new Event('submit', {cancelable: true, bubbles: true})); }, 100); }); console.log(res); } catch(e) { console.error(e); } finally { if(c) await c.close(); } } run();  
